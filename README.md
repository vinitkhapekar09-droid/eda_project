# EDA Project

This project focuses on exploratory data analysis (EDA) using Python.  
It includes visualizations, data cleaning, and insights from the dataset.

## Setup

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt